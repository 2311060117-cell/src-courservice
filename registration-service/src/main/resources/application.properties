server.port=8083
spring.application.name=registration-service

# Cấu hình kết nối MySQL DB
spring.datasource.url=jdbc:mysql://localhost:3003/registration_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=12345

# Cấu hình JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Khai báo Secret Key (Bắt buộc phải trùng với auth-service)
jwt.secret=CRS-Microservices-Secret-Key-Nam-3-Hoc-Ky-2026-Doi-Trong-Thuc-Te
jwt.expiration-ms=86400000

# URL kết nối tới course-service
course-service.url=http://localhost:8082